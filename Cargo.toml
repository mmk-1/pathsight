[package]
name = "pathsight"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "psight"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
